<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="popup.popfisher.com.smartpopupwindow" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".smart.PopupWindowMainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".smart.CustomPosPopupActivity" />
        <activity android:name=".smart.TopBottomArrowPopupActivity" />
        <activity android:name=".smart.Android7_0PopupActivity" />
        <activity
            android:name=".smart.ScreenChangeUpdatePopupActivity"
            android:label="ScreenChangeUpdatePopupActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:screenOrientation="unspecified" >
        </activity>
    </application>

</manifest>